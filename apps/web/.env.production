# Production ("vite build") API base URL, inlined into the bundle by Vite.
# Loaded automatically by Vite in production mode; dev keeps the localhost
# default from apps/web/src/lib/api/client.ts.
VITE_API_BASE_URL=https://zelora-api.farqas007.workers.dev